import styled from "styled-components";
import { ButtonCircle } from "../../particles/Buttons";
const ContactItem = styled.div``;
const ContactLabel = styled.label``;

export default function ContactItemFC() {
  return ContactItem;
}
